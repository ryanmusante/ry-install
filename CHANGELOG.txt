3.24.0 (2026-03-31)
-------------------
  docs(readme): restructure for professional clarity. Flatten ToC
    (remove 15 nested sub-entries), collapse pre-flight steps into
    collapsible block, consolidate Quick Start into single code block,
    normalize table separators, tighten phrasing. 560 → 539 lines.
  docs(readme/profile): strip box-drawing from example profile
    comments and remove 2 verbose inline comments; aligns with 3.23.0
    style change in script.
  docs: sync README (version badge 3.24.0), CHANGELOG.

3.23.0 (2026-03-31)
-------------------
  style: remove decorative box-drawing characters (══, ──) from 46
    comment headers; text and indentation preserved.
  docs: sync README (version badge 3.23.0), CHANGELOG.

3.22.0 (2026-03-30)
-------------------
  profile: remove PROTON_DXVK_LOWLATENCY=1 from ENV_VARS; proton-cachyos
    removed gplasync and PROTON_DXVK_LOWLATENCY is the only option, but
    upstream Valve Proton and GE-Proton ignore it. ENV_VARS 6 → 5.
  docs: sync README (version badge 3.22.0, env var table), CHANGELOG.

3.21.0 (2026-03-30)
-------------------
  fix(parallel): increase child stderr capture from 5 to 15 lines
    across 6 sites: _ry_validate_configs (2), _ry_verify_static (1),
    _ry_do_check (1), _ry_do_test_all (2). Improves crash diagnostics
    when multiple configs fail in a single child.
  docs(profiles): add collapsible example external profile to README
    with all 25 required globals, section comments, and validation
    commands.
  docs: sync README (version badge 3.21.0), CHANGELOG.

3.20.0 (2026-03-30)
-------------------
  style(profile): break PKGS_ADD (13) and PKGS_DEL (8) into
    multi-line continuations; matches all other ≥3-item lists.
  style(_json_str): trim 3-line comment to single line.
  style(_get_boot_time): add capture group extraction comment.
  docs: sync README (version badge 3.20.0), CHANGELOG.

3.19.0 (2026-03-30)
-------------------
  fix(_gather_cpu_state): remove dead _CPU_GOVERNOR and _CPU_EPP
    globals. Sysfs reads moved to data-driven loop in v3.8.0.
  docs: sync README (version badge 3.19.0), CHANGELOG.

3.18.0 (2026-03-30)
-------------------
  fix(wifi): remove 2 dead isatty stdin checks in
    _install_collect_wifi; unreachable after entry guard.
  fix(exit-codes): replace 4 bare return 2 with $EXIT_USAGE in
    _ry_do_diff and _ry_do_install_file.
  refactor(verify-static): pre-filter MASK for LVM before
    batch/fallback branch; eliminates 2 duplicate lvm2 checks.
  docs: fix README Finalize phase description, sync version badge
    3.18.0, CHANGELOG.

3.17.0 (2026-03-30)
-------------------
  fix(_gkeyfile_escape): add `string collect` to 2 string replace
    calls before \n escape (same class as _json_str F-1, v3.14.0).
  fix(wifi): SSID banned chars 15 → 5; drop 10 with no failure path.
  docs: sync README (version badge 3.17.0, WiFi prereq), CHANGELOG.

3.16.0 (2026-03-30)
-------------------
  fix(bootstrap): replace 6 bare exit 1 with exit $EXIT_PREFLIGHT in
    fish version gate, HOME resolution, and log dir creation.
  docs: sync README (version badge 3.16.0), CHANGELOG.

3.15.0 (2026-03-30)
-------------------
  fix(verify-static): add LVM-aware mask verification. Skip
    lvm2-monitor.service check when LVM detected via pvs.
  style: remove 5 consecutive blank line runs between functions.
  docs: sync README (version badge 3.15.0), CHANGELOG.

3.14.0 (2026-03-30)
-------------------
  fix(_json_str): add `string collect` to 4 pre-\n-escape string
    replace calls. Prevents silent newline corruption in JSONL logs.
  fix(parallel): add >/dev/null stdout redirect to all 10
    backgrounded fish -c children.
  docs: sync README (version badge 3.14.0), CHANGELOG.

3.13.0 (2026-03-29)
-------------------
  profile: add tsc=reliable kernel param. KERNEL_PARAMS 16 → 17.
  profile: add AMD_VULKAN_ICD=RADV, VKD3D_DEBUG=none to ENV_VARS.
  profile: remove PROTON_USE_NTSYNC=1, PROTON_NO_WM_DECORATION=1.
  profile: MESA_SHADER_CACHE_MAX_SIZE 8G → 4G. ENV_VARS 6 → 6.
  docs: sync README (version badge 3.13.0, env var table), CHANGELOG.

3.12.0 (2026-03-29)
-------------------
  profile: add split_lock_detect=off (16 params), AUR_PKGS
    (mt76-mt7925-dkms). Remove iwd DefaultInterface=* quirk.
  verify: remove E610 NVM check (board v2.2 uses RTL8127).
  verify: add AUR_PKGS to static verification.
  fix(logging): break _json_str → _log → _json_str circular call.
  docs: restructure README — add ToC, Install Flow, Known Issues
    tables, collapse jq examples. Sync version badge 3.12.0.

3.11.0 (2026-03-29)
-------------------
  profile: add PROTON_DXVK_LOWLATENCY=1, ntsync-common to PKGS_ADD.
  profile: remove udev rules file. Embedded files 15 → 14.
  verify-runtime: replace udev check with ntsync-common package check.
  docs: sync README (version badge 3.11.0, file/pkg counts).

3.10.4 (2026-03-27)
-------------------
  fix(install): propagate _atomic_write_file return code — previously
    always returned 0, silently swallowing write failures.
  fix(completions): add fish --no-execute syntax validation before mv.
  docs: sync README (version badge 3.10.4), CHANGELOG.

3.10.3 (2026-03-27)
-------------------
  fix(docs): renumber pipeline phase comments 1,3,4,5,7,8 → 1–6.

3.10.2 (2026-03-26)
-------------------
  fix(parallel): pre-serialize sudo-dependent I/O in parent for
    _ry_do_check and _ry_verify_static parallel children.
  fix(scope): 4 block-scoped variable shadows in _ry_do_diff,
    _install_configure_services, _ry_verify_runtime.

3.10.1 (2026-03-25)
-------------------
  fix(diff): batch I/O — pre-generate content + parallel reads.
  fix(check): pre-serialize LVM state for parallel Job 4.

3.10.0 (2026-03-25)
-------------------
  feat(verify-static): parallel hash workers (min(4, nproc)).
  feat(check): 4-job parallel check with pre-serialized state.

3.9.0 (2026-03-24)
------------------
  feat(validate): 5-job parallel config validation pipeline.
  refactor: extract _pregenerate_content_files, _ensure_sudo_cached.

3.8.4 (2026-03-23)
------------------
  refactor: extract _parse_systemctl_show, centralize lint exclusions,
    data-driven completions. Extract SYSCTL_CHECKS variable.

3.8.3 (2026-03-23)
------------------
  refactor(diff): remove delta as diff colorizer; use sed (standard).

3.8.2 (2026-03-23)
------------------
  fix(verify): split vendor-managed sysctl checks from ry-install-
    managed. kernel.split_lock_mitigate=0 → _warn (not _fail).

3.8.1 (2026-03-22)
------------------
  fix(scope): _pregenerate_content_files block-local out_dir shadow.
  fix(style): quote /etc/kernel/cmdline in SYSTEM_DESTINATIONS.

3.8.0 (2026-03-22)
------------------
  feat(kernel): add amdgpu.wbrf=0, tsc=reliable. KERNEL_PARAMS → 15.
  feat(env): add DXVK_LOG_LEVEL=none. ENV_VARS 4 → 5.
  feat(mask): add systemd-zram-setup@zram0.service. MASK 9 → 10.
  feat(verify): 12 new runtime checks — THP, KSM, ZRAM, amd_pstate,
    CPU boost, split_lock_mitigate, NVMe scheduler, BIOS VRAM.

3.7.56–3.7.59 (2026-03-21 — 2026-03-22)
---------------------------------------
  refactor(naming): prefix 22 public functions with _ry_.
  fix(scope): 16 block-scoped shadows across 10 functions.
  fix(security): post-write symlink re-check on WiFi tmpfile.
  fix(validate): drirc XML structure check (all files validated).
  feat(guard): re-source guard for duplicate event handlers.
  remove: _check_hardware_fingerprint, _btrfs_pre_snapshot.

3.7.0–3.7.55 (2026-03-08 — 2026-03-21)
--------------------------------------
  feat: batch & parallel processing (B-1..B-9). Fork cap at 16.
  feat(kernel): cwsr_enable=0, workqueue.power_efficient=0,
    ttm.pages_limit. Remove split_lock_detect=off.
  feat: sysctl VM tunables, resolved DoT/DNSSEC, modprobe configs,
    drirc unified heap, Mesa anti-lag env var.
  fix: 202 Bash-style &&/|| → Fish. 12 fish -c scope fixes.
    8 set scope leaks. 8 "if not" inversions.
  fix(security): amd_iommu=off → iommu=pt. _run metachar rejection.
  fix(signal): Fish signal name without SIG prefix. Exit 128+signum.
  fix(lock): PID ownership before delete. _do_cleanup race.
  fix(wifi): _gkeyfile_escape tab/newline. WPA2 wc -c (bytes).
  remove: --profile flag (now file-based only).

3.5.0–3.6.4 (2026-03-07 — 2026-03-08)
-------------------------------------
  feat: --check silent idempotency probe (exit 0/10).
  feat: structured exit codes (0–5, 10–11). Manifest orphan tracking.
  feat: --profile multi-machine support. _validate_profile.
  fix: fish version gate 3.4+. Root UUID cached once. Boot abort.

3.0.0–3.4.1 (2026-02-27 — 2026-03-06)
-------------------------------------
  feat: sysctl.d, --test-all, --completions, KERNEL_PARAMS validation,
    boot sanity, GKeyFile WiFi escaping, NDJSON header/footer.
  fix: 292+ scope fixes. 96+ bare cat/rm → command prefix. SIGPIPE
    handler. 2 infinite loops. do_diff --fix never triggered.
  remove: --no-color, --no-log, --clean (344 lines), --json, --once.
  docs: README rewrite. Adopt kernel.org changelog style.

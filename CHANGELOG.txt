3.14.0 (2026-03-30)
-------------------
  fix(_json_str): pipe 4 pre-newline-escape string replace calls
    through `string collect --no-trim-newlines` to prevent Fish
    command-substitution splitting. Embedded newlines were consumed
    by list splitting before the \n escape step, silently becoming
    spaces in JSONL log output (F-1: medium — silent data corruption).
    _gkeyfile_escape has the same pattern (L1140–1143) but is
    mitigated by upstream input validation rejecting newlines in
    SSID/passphrase.
  fix(parallel): add >/dev/null stdout redirect to all 10
    backgrounded fish -c children — 5 validation jobs (_ry_validate_configs),
    4 check jobs (_ry_do_check), 1 verify-static hash worker
    (_ry_verify_static). stderr was redirected but stdout was open;
    any unexpected Fish runtime diagnostic would interleave with
    parent output (F-2: low — cosmetic).
  docs: sync README (version badge 3.14.0), CHANGELOG.

3.13.0 (2026-03-29)
-------------------
  profile: add tsc=reliable kernel param after clocksource=tsc.
    Skips runtime TSC verification on Zen 5 invariant TSC.
    KERNEL_PARAMS 16 → 17.
  profile: add AMD_VULKAN_ICD=RADV to ENV_VARS. Ensures RADV
    over AMDVLK if installed as ROCm dependency.
  profile: add VKD3D_DEBUG=none to ENV_VARS. Disables VKD3D-Proton
    debug logging for DX12 games. Complements DXVK_LOG_LEVEL=none.
  profile: remove PROTON_USE_NTSYNC=1 from ENV_VARS.
    proton-cachyos 10.0-20260312-slr enables ntsync by default.
  profile: remove PROTON_NO_WM_DECORATION=1 from ENV_VARS.
    No effect under X11/XWayland; apply per-game when using
    Wayland. ENV_VARS 6 → 6 (+2 −2).
  profile: change MESA_SHADER_CACHE_MAX_SIZE 8G → 4G. Most games
    use 50–430 MB; 4G is generous.
  verify-static: remove PROTON_USE_NTSYNC=1 cross-check in ntsync
    unavailable branch (env var no longer in profile).
  docs: sync README (version badge 3.13.0, kernel param count 17,
    tsc=reliable row, env var table updated), CHANGELOG.

3.12.0 (2026-03-29)
-------------------
  profile: add split_lock_detect=off kernel param (16 params).
  profile: fix cwsr_enable=0 comment — remove when 7.0+ stable
    on gfx1151 (was 6.18+). 6.19.0 black screen regression.
  profile: add proton-cachyos-slr note to PROTON_DXVK_LOWLATENCY
    env var comment.
  profile: add AUR_PKGS global (mt76-mt7925-dkms). New
    _install_aur_packages function installs via paru.
  profile: remove iwd DefaultInterface=* DriverQuirk
    (NixOS #439914). PowerSaveDisable=* retained.
  verify: remove E610 NVM firmware check — board v2.2 uses
    Realtek RTL8127 (r8169 in-tree). Check was non-functional
    (matched ice driver, E610 uses ixgbe).
  verify: add AUR_PKGS to static verification (warn-level).
  deps: add paru to optional-tools warning block.
  fix(logging): break _json_str → _log → _json_str circular call
    chain. _json_str arg-count guard called _log, which calls
    _json_str — latent recursion (terminates at depth 2 but violates
    self-containment). Replace _log call with direct printf to
    $LOG_FILE guarded by test -f, matching _msg bug-path precedent
    (L1217). No callers affected.
  style(comments): trim 14 multiline comments to single line
    across 12 locations. No inline comments moved (all are
    lint:ignore directives or help text examples).
  docs: restructure README — add ToC, promote Quick Start,
    add Install Flow phase table, group config under single
    heading, break out Network Stack and System Tuning, convert
    Known Issues to status/workaround tables, collapse jq
    examples in <details>, move Hardware Reference to bottom.
  docs: fix 4 CHANGELOG underline length mismatches (3.7.56,
    3.7.0, 3.5.0, 3.0.0).
  docs: sync README (version badge 3.12.0, all counts verified),
    CHANGELOG.

3.11.0 (2026-03-29)
-------------------
  profile: add PROTON_DXVK_LOWLATENCY=1 to ENV_VARS — DXVK low-latency
    frame pacing for DX9/10/11 titles (proton-cachyos-slr ≥10.0-20260228).
  profile: add ntsync-common to PKGS_ADD — replaces manual ntsync udev
    rule with package-managed module loading and udev rule.
  profile: remove /etc/udev/rules.d/99-cachyos-udev.rules from
    SYSTEM_DESTINATIONS and _ry_get_file_content. Embedded file count
    15 → 14.
  profile: remove UDEV_RULES global and _validate_profile conditional.
  verify-runtime: replace udev rules file check with ntsync-common
    package presence check.
  verify-runtime: lower BIOS VRAM carveout threshold 1024 → 512 MB.
  docs: fix stale header comment v3.10.4 → v3.11.0. Sync README
    (version badge 3.11.0, embedded files 14, packages 13, env vars 7),
    CHANGELOG.

3.10.4 (2026-03-27)
-------------------
  fix(install): propagate _atomic_write_file return code in
    _ry_install_file. Previously always returned 0 after the atomic
    write call (line 2473), silently swallowing failures from mktemp,
    chmod, mv, or post-write hash mismatch. All 9 callers already use
    "if not _ry_install_file" — failures are now visible. Fix: replace
    bare "return 0" with "return $status".
  fix(completions): add fish --no-execute syntax validation on
    generated completions file before mv. Previous check only verified
    closing 'end' keyword was present. Catches template generation bugs
    that produce syntactically invalid Fish. Failure path cleans tmpfile.
  fix(docs): add security comment documenting why _run is intentionally
    bypassed for WiFi credential write (line 5653). printf|sudo tee
    prevents psk= from appearing in JSONL logs; tee stdout→/dev/null
    ensures credential never reaches _run's output capture path.
  docs: sync README (version badge 3.10.4), CHANGELOG.

3.10.3 (2026-03-27)
-------------------
  fix(docs): renumber pipeline phase comments 1,3,4,5,7,8 → 1–6.
    Phases 2 and 6 were gaps left from earlier refactors when WiFi
    collection moved from phase 2 to between boot and finalize.
    Pipeline header updated: preflight → packages → files →
    services → boot → wifi → finalize (was: preflight → wifi
    → packages → files → services → boot → finalize).
  fix(docs): add _banner layout math comment. Documents inner (68)
    = border codepoint width, max_text = inner - prefix(3) - suffix(2)
    = 63. Aids future maintainers modifying box dimensions.
  fix(validate): add udev operator correctness checks to Job 5.
    Assignment keys (MODE, GROUP, OWNER) must use single = not ==.
    Match keys (KERNEL, SUBSYSTEM, DRIVER, ACTION) must use == not
    single =. Previous validation only checked for empty trailing ==
    and assignment presence.

3.10.2 (2026-03-26)
-------------------
  style(verify-static): rename hooks_line → hooks_syntax_line in
    SYNTAX VALIDATION section (L3332). Eliminates scope shadow with
    same-named variable in CHECKSUM VERIFICATION section (L3088).
    Both are set -l in _ry_verify_static, 244 lines apart. No
    behavioral change — Fish scoping made this safe, but unique
    names improve grep-ability and audit clarity.

3.10.1 (2026-03-25)
-------------------
  fix(banner): inner width 66→68 — middle line now matches 68-char
    border. Was 2 chars short, causing misaligned right edge.
  fix(verify): _verify_unit_syntax captures systemd-analyze stderr
    to tmpfile and logs first 5 lines. Warnings were silently
    discarded via 2>/dev/null. Follows parallel job pattern (L2087).
  fix(completions): write verification now checks file is non-empty
    and contains closing 'end' keyword, not just $status of last
    echo. Catches intermediate write failures on full filesystem.

3.10.0 (2026-03-25)
-------------------
  remove(cli): --logs command — journal viewer and NDJSON analyzer.
    Removes _ry_do_logs, _logs_file_ops, _logs_journal, _analyze_log,
    _logs_list, _find_latest_log, _jsonl_data (616 lines). NDJSON
    logging to ~/ry-install/logs/ preserved. --test-all parallel_modes
    reduced by 7. --completions updated.
  refactor(progress): condense 19-step progress bar to 6 major
    operations — Preflight, Packages, Configuration, Services, Boot,
    Finalize. One step per pipeline function. 13 mid-function
    _progress calls removed, 3 _progress_skip calls consolidated to 1.
  docs: add Log Analysis section to README — NDJSON event reference
    table, 9 jq examples covering errors, failures, step timing,
    drift, interrupted runs. Replaces built-in --logs viewer.
  docs: sync README (options table, troubleshooting, install flow,
    log analysis), version badge 3.10.0. CHANGELOG.

3.9.0 (2026-03-24)
------------------
  remove(kernel): ttm.pages_limit=32505856 — OOM risk, leaves ~2.2 GiB
    outside GPU pool on 128 GB. pci=pcie_bus_perf — negligible benefit
    on modern kernels, can cause resource conflicts. tsc=reliable —
    bypasses TSC watchdog unnecessarily. KERNEL_PARAMS 15 → 13.
  feat(kernel): add clocksource=tsc — forces TSC without disabling
    instability detection. Replaces tsc=reliable.
  feat(kernel): add nvme_core.multipath=N — replaces modprobe option
    with kernel param for single-drive desktop.
  feat(kernel): add module_blacklist=pcspkr,wdat_wdt — replaces modprobe
    blacklists with kernel param. CachyOS covers iTCO/sp5100 only, not
    wdat_wdt or pcspkr. KERNEL_PARAMS 13 → 15.
  remove(sysctl): /etc/sysctl.d/99-ry-sysctl.conf — entire file
    redundant with CachyOS 70-cachyos-settings.conf. Removes
    SYSCTL_CHECKS global, _validate_profile sysctl case,
    _ry_get_file_content sysctl case, verify-static _chk_file/_chk_grep
    block (9 checks), verify-runtime /proc/sys loop, sysctl_files_fixed
    flag + match + reload prompt, --fix sysctl reload, --install-file
    sysctl reload. Vendor sysctl loop (split_lock_mitigate) preserved.
  remove(modprobe): /etc/modprobe.d/99-ry-modprobe.conf — pcspkr,
    wdat_wdt, nvme_core multipath=N migrated to kernel params.
    mt7925e disable_aspm=1 covered by existing kernel param.
    mt7925_common power_save=0 dropped (NM powersave=2 + cmdline cover
    main paths). Removes _ry_get_file_content modprobe case,
    verify-static block (5 checks). SYSTEM_DESTINATIONS 12 → 10.
    Embedded files 17 → 15.
  remove(udev): NVMe rq_affinity=2 + read_ahead_kb=128 rule. Removes
    verify-static rq_affinity _chk_grep. UDEV_RULES 2 → 1.
  remove(verify): NVMe I/O scheduler loop, btrfs noatime check (out of
    scope — fstab concern). nvme_core.multipath sysfs check preserved
    (now verifies kernel param).
  remove(dep): sysctl binary from dependency check — all sysctl --system
    calls removed; vendor loop reads /proc/sys via cat.
  docs: sync README — kernel params (15), embedded files (15), remove
    sysctl overrides section, remove modprobe row, update udev summary,
    fix anchor link, ROCm VRAM note, IWD DriverQuirks detail, MES
    firmware warning, Strix Halo Toolboxes + Ollama references,
    version badge 3.9.0. CHANGELOG.

3.8.9 (2026-03-24)
------------------
  fix(lint): remove 2>&1 from fish -n syntax check in _ry_do_lint.
    Syntax error diagnostics leaked to stdout, breaking the
    stdout=machine/stderr=human contract. stderr now stays on fd 2.
  refactor(logs): rename local exit_code to _log_exit in _logs_list,
    _analyze_log, _logs_file_ops. Eliminates scope shadow against the
    global exit_code used by main dispatch (L7296). No runtime bug
    (set -l properly scoped), but improves grep-based reasoning.
    JSON key "exit_code" in grep patterns preserved.
  docs: sync README (version badge 3.8.9), CHANGELOG.

3.8.8 (2026-03-24)
------------------
  feat(lint): add inline # lint:ignore suppression. Lines annotated with
    # lint:ignore are excluded from all anti-pattern checks (&&/||, $(),
    [[ ]], export, ${var}, dead grep -q) and scope shadow detection.
    Suppresses 5 false positives: 2 POSIX shell ||/&& in flock /bin/sh -c
    lock reclaim (lines 326-327), 1 awk && in scope shadow checker (line
    4897), 2 set -l in fish -c subshells (lines 2236, 6934). Works via
    sed '/# lint:ignore/d' on clean_content and awk /# lint:ignore/ { next }
    on scope shadow scanner. Comment syntax is safe in all three contexts:
    fish (#), POSIX sh (#), awk (#).
  docs: sync README (version badge 3.8.8, lint description), CHANGELOG.

3.8.7 (2026-03-24)
------------------
  fix(logs): _jsonl_data PCRE2 regex compile error on fish 3.7. Character
    class [^"\\] delivered only [^"\] to PCRE2 because fish single-quotes
    process \\ → \. Doubled escapes: [^"\\\\] so fish delivers [^"\\].
    Broke --logs analyze, --logs last, --logs all.
  fix(comment): correct _jsonl_data comment — fish single-quotes are NOT
    literal like bash (process \\ and \').
  style: remove 5 unnecessary quotes flagged by fish_indent (lines 495,
    505, 1071, 3308, 6831). fish_indent --check now passes.
  docs: sync README (version badge 3.8.7), CHANGELOG.

3.8.6 (2026-03-24)
------------------
  fix(wifi): check $pipestatus on WiFi credential printf|tee pipe.
    Consistent with _atomic_write_file pattern (line 2383). Old
    structure used "if printf|tee" which only checked tee exit;
    refactored to explicit $pipestatus[1] and $pipestatus[2] check
    with separate error/success paths. Removes orphaned else block.
  fix(verify): use sudo dmesg for ReBAR/SAM check in
    _ry_verify_runtime. Sudo already cached by _ensure_sudo_cached
    at function entry; unprivileged dmesg fails when
    kernel.dmesg_restrict=1.
  fix(verify): replace deprecated test -a with "; and test" in E610
    NVM version check (line 4518). Single remaining instance.
  docs: sync README (version badge 3.8.6), CHANGELOG.

3.8.5 (2026-03-24)
------------------
  fix(scope): _msg set -l level ERR inside if-block shadows
    --argument-names level. Block-local destroyed on end; _log,
    switch/case, set_color used original invalid level string. Fix:
    bare set (rebinds function-scope). 18th block-scope shadow
    (v3.7.58=16, v3.8.1=1, v3.8.5=1).
  docs: sync README (version badge 3.8.5), CHANGELOG.

3.8.4 (2026-03-24)
------------------
  fix(logging): rm LOG_FILE before _load_profile exits. Prevents orphan
    footer-only log on profile configuration errors (matches arg-parsing
    cleanup pattern).
  fix(diff): add missing sysctl reload in --diff --fix. Re-installing
    /etc/sysctl.d/99-ry-sysctl.conf now offers sysctl --system reload,
    matching --install-file behavior.
  refactor(install): extract _atomic_write_file helper. Deduplicates
    sudo/non-sudo atomic write logic in _ry_install_file (~80 lines).
    Non-sudo chmod/mv now logged via _run for consistency.
  refactor(check): pre-serialize systemctl show output in parent.
    Eliminates duplicated _parse_systemctl_show in fish -c child.
  refactor(profile): extract SYSCTL_CHECKS variable. Single source of
    truth for sysctl runtime verification values. Conditionally required
    when profile includes sysctl.d destination.
  refactor(completions): replace nested single-quote escaping with
    data-driven echo loop. Same output, maintainable entries.
  refactor(lint): centralize output function exclusion list. Single
    _output_funcs variable for 5 grep -vE filters. Adds _dry/_msg to
    previously incomplete exclusions.
  docs: sync README (version badge 3.8.4), CHANGELOG.

3.8.3 (2026-03-23)
------------------
  refactor(diff): remove delta (Rust) as preferred diff colorizer. Diff
    display now uses sed colorization (standard tool, always available).
    Delta remains in PKGS_ADD for user's interactive use (git diff, etc.)
    but is no longer invoked by ry-install internally. Removes HAS_DELTA
    global. Consistent with policy: script uses standard tools only,
    Rust tools are installed for user convenience.
  docs: sync README (version badge 3.8.3, diff description), CHANGELOG.

3.8.2 (2026-03-23)
------------------
  fix(verify): split vendor-managed sysctl checks from ry-install-managed.
    kernel.split_lock_mitigate=0 (CachyOS 70-cachyos-settings.conf) now
    uses _warn on mismatch instead of _fail. Prevents false FAIL in
    --verify-runtime on systems where CachyOS hasn't set the value.
    ry-install-managed checks (9 from 99-ry-sysctl.conf) remain _fail.
  docs: sync README (version badge 3.8.2), CHANGELOG.

3.8.1 (2026-03-22)
------------------
  fix(scope): _pregenerate_content_files set -l out_dir inside if block
    creates block-local destroyed on end — out_dir reverts to empty,
    test -d fails, return 1. Broke _ry_validate_configs (install aborts
    at preflight) and _ry_do_check (always returns EXIT_DRIFT). 17th
    block-scope shadow missed by v3.7.58 sweep. Fix: bare set (rebinds
    function-scope local from --argument-names).
  fix(style): quote /etc/kernel/cmdline in SYSTEM_DESTINATIONS for
    consistency with all 11 other entries. No functional change (Fish
    has no word splitting).
  docs: sync README (version badge 3.8.1).

3.8.0 (2026-03-22)
------------------
  feat(kernel): add amdgpu.wbrf=0 (P1 — disable WiFi RFI memory clock
    throttling on UMA) and tsc=reliable (P2 — skip TSC validation on
    Zen 5). KERNEL_PARAMS now 15.
  feat(env): add DXVK_LOG_LEVEL=none to ENV_VARS. ENV_VARS 4 → 5.
  feat(mask): add systemd-zram-setup@zram0.service. MASK 9 → 10.
  feat(udev): add NVMe rq_affinity=2 + read_ahead_kb=128. UDEV_RULES
    1 → 2.
  feat(sysctl): add tcp_notsent_lowat, dirty_expire_centisecs,
    dirty_writeback_centisecs. Sysctl entries 6 → 9.
  feat(modprobe): add mt7925_common power_save=0. Modprobe entries
    4 → 5.
  feat(verify): add 12 new _ry_verify_runtime checks — E610 NVM
    firmware ≥ 1.30 (P0), THP, KSM, ZRAM, amd_pstate, CPU boost,
    split_lock_mitigate, NVMe scheduler, BIOS VRAM carveout.
  feat(static): add _chk_grep for 3 new sysctl, mt7925_common modprobe,
    NVMe udev rq_affinity.
  docs: sync README — kernel params (15), sysctl (9), version badge.

3.7.56-3.7.59 (2026-03-21 -- 2026-03-22)
----------------------------------------
  refactor(naming): prefix 22 public functions with _ry_ to prevent
    autoloaded function collisions. External profiles accept both
    _ry_profile_<n> (preferred) and legacy profile_<n> (deprecated).
  fix(scope): 16 block-scoped set -l shadows across 10 functions.
    Fix: remove -l from 26 inner set statements.
  fix(scope): add explicit -l flag to 28 bare set rebinds inside
    functions. Eliminates do_lint scope warnings.
  fix(regex): sdboot-manage.conf LINUX_OPTIONS extraction — strict
    '"([^"]*)"' replaces lazy '(.*?)"?'.
  fix(security): add post-write symlink re-check to WiFi NM connection
    tmpfile write path (closes TOCTOU gap).
  fix(logs): _jsonl_data helper for escaped quotes in JSONL data field.
    Sudo guard for gpu/usb/kernel dmesg.
  fix(validate): drirc XML structure check (all 17 embedded files now
    validated).
  feat(guard): re-source guard prevents duplicate event handler
    registration if sourced twice in same fish session.
  remove(preflight): _check_hardware_fingerprint, _btrfs_pre_snapshot.
  docs: sync README (version badge, profiles section).

3.7.0-3.7.55 (2026-03-08 -- 2026-03-21)
---------------------------------------
  feat: batch & parallel processing (B-1..B-9). Parallel collector with
    deterministic output ordering. Fork cap at 16.
  feat(kernel): add cwsr_enable=0 (gfx1151), workqueue.power_efficient=0,
    ttm.pages_limit=32505856. Remove split_lock_detect=off.
  feat(sysctl): VM gaming tunables, max_user_instances, max_map_count.
  feat(resolved): DNSOverTLS=opportunistic, DNSSEC=allow-downgrade.
  feat(modprobe): 99-ry-modprobe.conf — pcspkr, wdat_wdt, nvme_core,
    mt7925e.
  feat(drirc): add /etc/drirc with radv_enable_unified_heap_on_apu.
  feat(env): add ENABLE_LAYER_MESA_ANTI_LAG=1 (Mesa 25.3). Remove
    AMD_VULKAN_ICD=RADV (AMDVLK discontinued Sept 2025).
  feat(preflight): Intel E610-XT2 10GbE detection, BIOS VRAM check.
  feat(verify): btrfs noatime, ntsync builtin /dev/ntsync.
  fix: 202 Bash-style &&/|| → Fish "; and"/"; or". 12 double-quoted
    fish -c blocks. 8 set scope leaks. 8 Fish "if not <cmd>" inversions.
  fix(check): false drift on masked services, pvs timeout, timer-aware
    fstrim, Fish {$phase} brace interpolation.
  fix(security): amd_iommu=off → iommu=pt. _run metacharacter rejection
    extended. Credential redaction.
  fix(signal): Fish signal name without SIG prefix. _cleanup exit
    130 → 128+signum.
  fix(lock): PID ownership before lock delete. _do_cleanup race.
  fix(log): LOG_FILE rename race, truncation mid-escape split, atomic
    install -m 0600. Log rotation excludes current file.
  fix(wifi): _gkeyfile_escape tab/newline. SSID allows '!'/'&'. WPA2
    passphrase: string length → wc -c (bytes). md5sum UUID fallback.
  fix(service): amdgpu DPM level high→auto, After=multi-user.target.
  fix(sysctl): remove max_user_watches, fq/bbr overrides, zone_reclaim.
  fix(logind): add 3 missing long-press keys (completes all 8).
  remove: --profile flag. Profile selection now file-based only.

3.5.0-3.6.4 (2026-03-07 -- 2026-03-08)
--------------------------------------
  feat: --profile multi-machine support. _validate_profile (25+
    required globals), external loading.
  feat: --check silent idempotency probe (exit 0=clean, 10=drift).
  feat: structured exit codes (0-5, 10-11). Manifest-based orphan
    tracking. Hardware mismatch warning.
  refactor: decompose do_diagnose (604L → orchestrator + 6 helpers)
    and do_logs (285L → dispatcher + 2 helpers).
  fix: fish version gate 3.3+→3.4+. Root UUID cached once. Abort on
    boot rebuild failure. do_check false drift on vfat ESP.
  remove: backup system remnants.

3.0.0-3.4.1 (2026-02-27 -- 2026-03-06)
--------------------------------------
  feat: sysctl.d/99-ry-sysctl.conf. --test-all suite. --completions.
    KERNEL_PARAMS validation against /proc/config.gz. Boot sanity.
    --existence-only hook validation. _verify_unit_syntax --user.
  feat: hardware fingerprint. GKeyFile WiFi escaping. --logs list/last/
    all. NDJSON header/footer. Scope shadow lint check.
  refactor: KVER globals, _ntsync_state (5 states). power-profiles-
    daemon from PKGS_DEL to MASK. Sudo keepalive, _log default event.
  fix: 292+ scope fixes across 33+ functions. 96+ bare cat/rm/mv/chmod
    → command prefix. SIGPIPE handler. Footer double-write race. Temp
    file pattern, sudo tee --, kill -0 -- separators. 2 infinite loops
    in _install_finalize. do_diff --fix never triggered. install_file
    skip-unchanged broken for 14/17 sudo-path files.
  remove: sp5100_tco blacklist, btusb.enable_autosuspend, modules-load.d.
    --no-color, --no-log, --clean (344 lines), _GPU_BUSY_PCT, --json,
    --once. Kernel params 19→18, files 17→16.
  docs: README rewrite with structured tables.
  chore: adopt kernel.org changelog style. Trim CHANGELOG to 3.0.0+.
